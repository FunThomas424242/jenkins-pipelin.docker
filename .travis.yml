sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t funthomas424242/jenkins-pipeline .
#  - docker run -d -p 127.0.0.1:80:4567 funthomas424242/jenkins-pipeline /bin/sh -c "cd /root/jenkins-pipeline; bundle exec foreman start;"
#  - docker ps -a
#  - docker run funthomas424242/jenkins-pipeline /bin/sh -c "cd /root/jenkins-pipeline; bundle exec rake test"

# script:
#  - bundle exec rake test
